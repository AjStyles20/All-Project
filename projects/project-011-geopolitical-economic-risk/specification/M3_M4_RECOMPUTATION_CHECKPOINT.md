# P003 M3/M4 — Vertical Slice and Recomputation Checkpoint

## M3 verification

GitHub Actions P003 ETEC Tests run #21 verified the F1 end-to-end vertical slice: **11 passed, 0 failed in 0.02s**.

M3 is therefore closed for the bounded constructed F1 slice.

## M4 objective

Counterevidence, contradiction and staleness must alter pathway state and force deterministic recomputation. They must not exist only as explanatory prose.

The first M4 implementation supports explicit revision of one existing transition assessment followed by re-evaluation of the complete weakest-link progression.

Initial deterministic cases:
- exposure becomes CONDITIONAL after diversification counterevidence;
- domestic-transmission evidence becomes STALE;
- authoritative reversal makes the event CONTRADICTED;
- every revision requires a preserved reason.

## Boundary

These are state-machine fixtures. They do not establish that any real-world source is credible counterevidence or that the chosen economic transition is empirically correct.
